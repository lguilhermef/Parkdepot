export enum BrowserStorage {GET_USER = "user"};

export enum AppMessageType {ERROR = "errorMessage", SUCCESS = "successMessage"};

export enum PermissionType {MANAGER = "MNG", EMPLOYEE = "EMP", GUEST = "GST"};